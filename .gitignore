.vercel
.DS_Store
*-monitor

.cursor

# Real secrets — never commit. Templates live next to each .env as .env.example.
.env
.env.local
.env.*.local

# Per-environment config (managed by parent repo's scripts/textbee/use_env.py).
# Canonical .env paths above are symlinks into env/<active>/.
/env/
!/env/.gitkeep